# A file to code the database models, creating models to store login, images, descriptions, passwords, etc...
from . import db

# importing the database from the __init__.py, the "from ." means we will import from a file located at this same folder
